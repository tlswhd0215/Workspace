#include <stdio.h>
int main()
{
	int number, no;
	char ch;
	if (number > 1 || number < 10);
	if (ch != 'q'&&ch != 'z');
	if (no != 5 && no > 1 && no < 10);
	if (no > 10 || no < 1);
	if (ch == ' ' || ch == '\n');
		return 0;
}